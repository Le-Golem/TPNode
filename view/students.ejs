<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Page d'accueil</title>
  <link rel="stylesheet" href="/style">
</head>
<body>
<div class="oui">
  <h1>Bienvenue!</h1>
  <button ><a class="button" href="/Form">Form</a></button>
</div>
  <p>Voici la liste des students :</p>
  <ul class="listes">
    <% students.forEach(students => { %>
      <% let i = 0 %>
      <li class="liste" id=i>
        <p><%= students.name %> <button href="/delete" class="sup">X</button></p>
        <p>date de naissance : <%= students.birth %></p>
      </li>
    <% }); %>
  </ul>
</body>
</html>
